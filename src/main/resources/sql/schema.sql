CREATE TABLE SHOPPING_BASKET (
  id INT NOT NULL AUTO_INCREMENT,
  PRIMARY KEY (id)
);

CREATE TABLE FOOD (
  id INT NOT NULL AUTO_INCREMENT,
  cart_id INT,
  name VARCHAR(255) NOT NULL ,
  cost FLOAT NOT NULL,
  FOREIGN KEY (cart_id) REFERENCES SHOPPING_BASKET(id),
  PRIMARY KEY (id)
);

CREATE TABLE USER (
  id INT NOT NULL AUTO_INCREMENT,
  username VARCHAR(40) NOT NULL,
  password VARCHAR(40) NOT NULL,
  first_name VARCHAR(40) NOT NULL,
  last_name VARCHAR(40),
  enabled TINYINT NOT NULL DEFAULT 1,
  role VARCHAR(15) NOT NULL DEFAULT 'ROLE_USER',
  phone_number LONG,
  address VARCHAR(250),
  PRIMARY KEY (id)
);


CREATE TABLE ORDERS (
  id INT NOT NULL AUTO_INCREMENT,
  user_id INT,
  basket_id INT,
  amount DOUBLE,
  FOREIGN KEY (user_id) REFERENCES USER(id),
  FOREIGN KEY (basket_id) REFERENCES SHOPPING_BASKET(id),
  PRIMARY KEY (id)
);


# CREATE TABLE SHOPPING_BASKET_DETAIL(
#   basket_id INT,
#   food_id INT,
#   PRIMARY KEY (basket_id,food_id),
#   FOREIGN KEY (basket_id) REFERENCES SHOPPING_BASKET(id),
#   FOREIGN KEY (food_id) REFERENCES FOOD(id)
# );